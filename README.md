# StoryPixie

StoryPixie is an AI-powered multimodal storytelling agent that generates stories using text, images, audio narration, and video.

## Project Structure

- `frontend/`: Next.js frontend application.
- `backend/`: FastAPI backend service.
- `deploy/`: Deployment configurations for Google Cloud Run.
- `architecture/`: System architecture documentation.
